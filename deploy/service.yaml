apiVersion: v1
kind: Service
metadata:
  name: llm4s-safe-deployment
spec:
  selector:
    app: llm4s-safe-deployment
  ports:
    - port: 80
      targetPort: 8080
      name: http
  type: ClusterIP
