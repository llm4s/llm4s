# Example dev config for policy-as-code CI checks (bundled resource).
# No API key required when using Ollama.
llm4s {
  llm {
    model = "ollama/llama3"
  }
  ollama {
    baseUrl = "http://localhost:11434"
  }
}
