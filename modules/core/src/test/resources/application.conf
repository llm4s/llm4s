llm4s {
  providers {
    provider = "deepseek-main"
    deepseek-main {
      provider = "deepseek"
      model = "deepseek-chat"
      apiKey = "test-key"
      # baseUrl intentionally omitted to rely on reference.conf default
    }
  }
  tools {
    brave {
      apiKey = "test-key"
    }
  }
}
