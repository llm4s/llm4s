llm4s {
  providers {
    provider = "openai-main"
    openai-main {
      provider = "openai"
      model = "gpt-4o"
      apiKey = "test-key"
      # baseUrl intentionally omitted to rely on reference.conf default
    }
  }
  tools {
    brave {
      apiKey = "test-key"
    }
  }
}
