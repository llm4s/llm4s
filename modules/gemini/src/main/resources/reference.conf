# llm4s-gemini defaults. HOCON merges this with llm4s-core's reference.conf, so
# these keys exist exactly when the Gemini and Vertex AI providers are on the
# classpath (#1132).
llm4s {
  providers {
    # Named Gemini and Vertex AI providers. Chat config is keyed by the instance
    # name you choose, so these can only be examples - there is no key to bind.
    #
    # gemini-main {
    #   provider = "gemini"            # "google" is accepted as an alias
    #   model = "gemini-2.0-flash"
    #   apiKey = ${?GEMINI_MAIN_API_KEY}
    #   baseUrl = ${?GEMINI_MAIN_BASE_URL}
    # }
    #
    # Vertex AI repurposes three fields: `endpoint` is the GCP project id,
    # `organization` the region (default us-central1), and `apiKey` an optional
    # path to a service-account credential file - it authenticates with OAuth2.
    #
    # vertexai-main {
    #   provider = "vertexai"          # "vertex" is accepted as an alias
    #   model = "gemini-2.0-flash"
    #   endpoint = ${?VERTEXAI_MAIN_PROJECT_ID}
    #   organization = ${?VERTEXAI_MAIN_LOCATION}
    #   apiKey = ${?GOOGLE_APPLICATION_CREDENTIALS}
    # }
  }
}
