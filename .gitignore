.idea
.venv
.env*
.DS_Store
.bloop/
project/.bloop/
.env.local
/.bsp/
target/
.aider*
.metals
.vscode
**/.claude/settings.local.json
# Agent worktrees are created inside the repo by Claude Code's worktree isolation.
# They are working copies, never content to commit.
**/.claude/worktrees/
.DS_Store
.log
.scala-build

# Benchmark data (downloaded datasets, generated test sets, results)
data/

# Developer local overlays
**/application.local.conf

# llm4s-pr-manager artifacts (anchored to repo root so they don't match nested files,
# e.g. docs/review-*.md). Personal-tooling patterns; prefer a global ~/.config/git/ignore
# for anything not shared.
/Pull requests/
/*_out.json
/test_log*.txt
/init_*.json
/generate_*.json
/review*.md
